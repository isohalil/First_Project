package day12arraylists;import java.util.ArrayList;import java.util.List;public class ArrayList02 {    public static void main(String[] args) {        //Örnek : Tekrarli elemanlari olan bir listten sadece tekrarsiz elemanlari olan bir list elde edinn        //           [J, a, v, a, v] ==> [J, a, v]        ArrayList <Character> newAges = new ArrayList<>();        newAges.add('J');        newAges.add('a');        newAges.add('v');        newAges.add('a');        newAges.add('v');        System.out.println(newAges); // [J, a, v, a, v] böyleydi once        ArrayList <Character> b = new ArrayList<>();        for (  Character w :  newAges ) {            if (!b.contains(w)){                b.add(w);            }        }        System.out.println(b); // [J, a, v] oldu sonraki                //Örnek : Bir String ArrayList oluşturun ve "a" içeren elemanlari silin.        ArrayList <String> r = new ArrayList<>();        r.add("Manisa");        r.add("Nigde");        r.add("Tokat");        r.add("Van");        System.out.println(r); // [Manisa, Nigde, Tokat, Van]        /*                // foreach kullandiginizda listelerde eleman sayisni azaltamazsiniz;                for ( String w : r ) {                    if (w.contains("a")){                        r.remove(w);                    }                }                System.out.println(r);             */        for (int i = 0; i < r.size(); i++) {            if (r.get(i).contains("a")){                r.remove(i); //remove() methodunu index ile de kullanabiliriz                i--; //  silme islemi oldugunda indexler azalacagi icin i degerini  de  azaltmaliyiz;            }        }        System.out.println(r);        //--------------------//-------------------//---------------------//------------------------//------------------        List<Character> o = new ArrayList<>();        o.add('K');        o.add('a');        o.add('l');        o.add('a');        o.add('l');        List<Character> m = new ArrayList<>();        for ( Character  w : o  ) {            if (!m.contains(w)){                 m.add(w);            }        }        System.out.println(m); // [K, a, l]        //Örnek : Bir String ArrayList oluşturun ve "a" içeren elemanlari silin.        ArrayList<String> h = new ArrayList<>();        h.add("Ankara");        h.add("Bartin");        h.add("Sakarya");        h.add("Mersin");        System.out.println(h); // [Ankara, Bartin, Sakarya, Mersin]                        /*                        for ( String w : h ) {                                if (w.contains("a")){                                    h.remove(w);                                }                        }                        System.out.println(h);                        */        for (int i = 0; i < h.size() ; i++) {            if (h.get(i).contains("a")){                h.remove(i);                i--;            }        }        System.out.println(h); // [Mersin]    }}